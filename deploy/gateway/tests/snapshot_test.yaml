suite: Snapshot
excludeTemplates:
  # TLS certificates are dynamic so these templates are tested separately.
  - NOTES.txt
  - tls-secret.yaml
tests:
  - it: should render
    set:
      metrics:
        podMonitor:
          enabled: true
    asserts:
      - matchSnapshot: {}
