{{- if include "gateway.createCertificate" . }}
{{- $cm := .Values.tls.autoGenerated.certManager }}
{{- $fullname := include "gateway.fullname" . }}
{{- $resourceAlias := include "gateway.resourceAlias" . }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ printf "%s-crt" $fullname }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
spec:
  secretName: {{ include "gateway.tlsSecretName" . }}
  duration: {{ $cm.duration }}
  renewBefore: {{ $cm.renewBefore }}
  commonName: {{ $fullname }}
  subject:
    organizations:
      - Twingate
  privateKey:
    algorithm: {{ $cm.keyAlgorithm }}
    size: {{ $cm.keySize }}
  dnsNames:
    - kubernetes
    - kubernetes.default
    - kubernetes.default.svc
    - {{ printf "kubernetes.default.svc.%s" .Values.clusterDomain }}
    {{- if ne $resourceAlias "" }}
    - {{ $resourceAlias }}
    {{- end }}
    {{- range .Values.tls.dnsNames }}
    - {{ . }}
    {{- end }}
  {{- with .Values.tls.ipAddresses }}
  ipAddresses:
    {{- range . }}
    - {{ . }}
    {{- end }}
  {{- end }}
  issuerRef:
    name: {{ printf "%s-ca-issuer" $fullname }}
    kind: Issuer
{{- end }}
